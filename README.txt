Инструкция:
🤖 Инструкция по управлению ботом (Журнал)

Папка бота: D:\AI\Text\Bots\Журнал\magazine_bot
1. Как изменить тексты и кнопки

Логика бота, тексты сообщений и кнопки находятся в файле:
📄 fsm_config.yaml

Это машина состояний без иерархии.
Правила редактирования:

    Редактор: Рекомендуется использовать Sublime Text или «Блокнот» Windows 11.

    Отступы: YAML критичен к отступам! Используйте только пробелы (обычно 2 или 4). Никогда не используйте клавишу Tab, иначе бот не запустится.

    Спецсимволы:

        Чтобы сделать текст жирным: <b>Текст</b>

        Чтобы добавить перенос строки: просто пишите с новой строки (если стоит символ | в начале) или ставьте \n.

2. Как применить изменения

После сохранения файла fsm_config.yaml изменения не появятся мгновенно. Бот нужно перезагрузить.

    Найдите в папке файл:
    ⚙️ перезапустить_бота.bat

    Запустите его двойным кликом.

    Разрешите права Администратора (нажмите «Да»).

    Дождитесь сообщения «Готово. Окно можно закрывать».

3. Если бот перестал отвечать

Если после редактирования и перезагрузки бот молчит, скорее всего, нарушен формат YAML (лишний пробел или забытая кавычка).

Как проверить:

    Откройте файл логов в папке бота:
    📄 bot_log_internal.log (откройте Блокнотом).

    Прокрутите в самый низ.

    Если видите ошибку ScannerError или ParserError — проблема в структуре YAML файла. Исправьте отступы и перезагрузите снова.

Совет: Перед сложными правками сделайте копию файла fsm_config.yaml, чтобы всегда можно было вернуться к рабочей версии.